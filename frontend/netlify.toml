[build]
  base = "frontend"
  command = "cd frontend && npm install --legacy-peer-deps && ng build --configuration production"
  publish = "frontend/dist/frontend/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
