import { CurrencyEsPipe } from './currency-es-pipe';

describe('CurrencyEsPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyEsPipe();
    expect(pipe).toBeTruthy();
  });
});
